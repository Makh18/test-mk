/**
 * Get the last n data elements from the nested object
 *
 * See the test if you have questions
 */
export default function getLastNumbers(x){
    for (let j=0; j < x; j++){
     for(let i = 0, l = data.items.length; i < l; i++) {
        return(data.items[i])
    }
}
}
